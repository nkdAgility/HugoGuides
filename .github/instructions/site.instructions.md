---
applyTo: "examples/reference-guide-site/**"
---

## Site Overview

This site is a demo site in Hugo for the Hugo module, allowing local debugging and development. It contains its own example guides and uses the local platform module. Consumer compatibility is tested separately against pinned site snapshots; do not mount consumer content into this reference site.
